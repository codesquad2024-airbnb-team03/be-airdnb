spring:
  application:
    name: airdnb
  config:
    import:
      - db-config.yml
      - authentication-config.yml
      - kakao-config.yml
      - multipart-config.yml
  oauth2:
    login:
      success-url: "http://localhost:3000/loginSuccess?token="
  cors:
    allowed-origins: "http://localhost:3000/"
  profiles:
    include: private
